# Go public — the plan for a fresh session (written 2026-09-22, night)

## Phase C report — 2026-09-23 (builder C, branch `docs/go-public-phase-c`)

**The public repository is clean and the install works from the README.** No BLOCKER and
no MAJOR. Six MINOR items, listed at the end; none is urgent. Your next moves:

1. **Merge this PR.**
2. **Optional, one minute:** edit your comment on #73. It still shows your home path, and it
   is public now (C1).
3. **The site session:** the install block is drafted at the end of this report.

**When it went public.** The unauthenticated API gives the repository's `updated_at` as
**2026-09-23 19:50:30Z (13:50 MDT)**. That is after the last push (19:46:54Z) and before
my first anonymous clone succeeded at **19:54:33Z (13:54 MDT)**. So it was public by
19:54:33Z at the latest, and the flip was probably at 19:50:30Z. The API now reports
`"visibility": "public"`, the MIT license, and Phase A's description, homepage and eight
topics.

**What was checked:** an anonymous clone with the PR heads; both scanners, compared with
Phase A; a grep for key shapes, emails and home paths; the public PR bodies and comments;
every link in `README.md`, `CONTRIBUTING.md`, `docs/README.md` and `docs/QUICKSTART.md`,
logged out; the npm metadata and the npm README's images; and an install walk from the
README in a throwaway home directory.

### C1. Anonymous clone and scanners

**The clone.** `git clone https://github.com/mlapeter/counterparts.git` ran with the global
and system git config switched off (`GIT_CONFIG_GLOBAL=/dev/null`, `GIT_CONFIG_NOSYSTEM=1`,
an empty `credential.helper`, `GIT_TERMINAL_PROMPT=0`). No credential helper or token could
answer, and it succeeded. HEAD is `46cf239`: 916 commits on master, 940 across every public
ref. The public refs are master, 195 PR heads, the tag `floor/v5-last` (its commit is on
master) and five branches: `dashboard/flat`, `design/parallel-run-contract`,
`docs/2026-09-18-morning`, `keyless/surfaces` and `keyless/write-up`. The last two belong to
the open PRs #195 and #192, and they moved while I worked.

**Tree scan** (`tools/audit/scan-personal.ts`). Three columns: Phase A's "after", taken on
its branch head; the Phase A merge `bf9be2d`, which adds Phase A's own report and #184's
ROADMAP; and the public HEAD.

| class | Phase A after | `bf9be2d` | public `46cf239` |
|---|---|---|---|
| owner-handle | 20 | 21 | 20 |
| owner-name | 121 | 123 | 121 |
| owner-email | 0 | 0 | 0 |
| owner-site | 2 | 2 | 2 |
| absolute-home-path | 14 in 9 files | 19 in 10 | **24 in 12** |
| tilde-path | 968 | 972 | 931 |
| live-store-path | 955 | 957 | 909 |
| uuid-session-id | 6 | 6 | **7** |
| short-session-id | 3 | 3 | 3 |
| run-id | 8 | 8 | 8 |
| scratchpad-path | 0 | 0 | 0 |
| claude-projects-path | 2 | 2 | 2 |
| **total** | **2,099 in 153 files** | **2,113 in 154** | **2,027 in 160** |

I read every hit that is new since `bf9be2d`. None is a problem:
- **absolute-home-path, +5.** Four are in `test/secrets-aws.test.ts:142-145`, placeholder
  paths in the AWS-key false-positive fixtures. One is `test/stop-ask-quiet.test.ts:593`, a
  placeholder `owner` user. All 24 hits are placeholder users, and five of them are Phase
  A's report quoting placeholders. None is the owner's account.
- **uuid-session-id, +1.** `test/stop-ask-quiet.test.ts:45` is the fixture UUID that three
  other test files already use.
- **tilde-path, +14, and live-store-path, +9.** These are:
  - the rewritten QUICKSTART, naming the default store and `.claude/settings.json`;
  - source comments in `cli/keys.ts`, `mcp/NOTES.md` and `claude-code/NOTES.md`;
  - the embedder research page, naming a local model folder;
  - `embed/static.ts`, reading a JSON field called `counterparts`;
  - `docs/ROADMAP.md:29`, which names the owner's private session-notes folder and says it
    is private. That is fine under the tilde-path ruling.
- **The drops** (tilde −55, live-store −57) are the short README and QUICKSTART from #194.

**Grep.** No email address in the tree beyond two `db.example.com` fixtures and two
`mcp-server@<port>.json` file names. There are zero hits for the owner's account under
`/Users/`. Every string with the Anthropic key prefix is one of four things: a test fixture,
a canary in an adversarial review, the prefix constant in `cli/keys.ts`, or this page quoting
the Phase A table. Every string with the AWS key-id prefix is AWS's documented example key,
in tests, in the encode gate or in its notes.

**History scan** (`tools/audit/scan-history.ts`):

| run | blobs | MB | with findings | binary, skipped | non-entropy hits |
|---|---|---|---|---|---|
| the plan's baseline (dev repo, 872 commits, `78c63e4`) | 3,630 | 185 | 2,388 | — | — |
| public clone, branches only | 3,846 | 193.9 | 2,572 | 12 | 1,274 in 664 blobs |
| public clone plus 195 PR heads | 3,852 | 194.3 | 2,578 | 12 | 1,274 in 664 blobs |
| `--worktree` at `bf9be2d` | 366 | — | 189 | — | 57 |
| `--worktree` at public HEAD | 386 | 13.0 | 197 | 3 | 67 in 34 files |

- **The PR heads add one blob** that no branch reaches. It is a `docs/IMPROVEMENTS.md`
  version at the head of #103 (2026-09-14). Its only hit is entropy, on a v1 path string.
  The other five blobs of the difference come from the two `keyless/*` branches moving
  between runs.
- **Since the plan's baseline,** 327 new blobs have findings. Their non-entropy hits fall in
  24 paths, 12 of them outside `test/`: Phase A's report quoting the two fixture names, and
  eleven `src/` files where the hit is a variable called `credentials`, `token` or
  `hasCredential`, or the gate's own documentation. **Zero true positives,** as on 09-04 and
  09-22.
- **`--worktree` went from 57 to 67.** The new `test/secrets-aws.test.ts` accounts for 4 and
  the other tests for 3. Four are credential-named variables in `embed/static.ts`,
  `claude-code/vectors.ts`, `encode/secrets.ts` and `encode/NOTES.md`. QUICKSTART lost one.
- **The 12 skipped binaries** are every version of the three dashboard PNGs. All date from
  2026-09-04 and were made from the synthetic demo store. I looked at the current overview:
  it shows `fernbrook-demo`, with a fictional clinic and fictional names.

**PR bodies and comments** (now public; read through the anonymous API). There are 195 PRs,
50 conversation comments, 0 review comments and 0 issues. **One hit matters:** your comment
on #73 (id `5549406590`, 2026-09-05) still quotes a stderr line with your absolute home path.
Phase A's A5 found it, and it has not been edited. The other hits are #195's body, naming
placeholder paths, and two of your comments on #189, quoting AWS's documented example key.

### C2. Links, logged out

Every request used `curl` with no cookies and no auth.

| link | where | status |
|---|---|---|
| `https://bun.sh` | README, QUICKSTART, CONTRIBUTING | 200 |
| `https://bun.sh/install` | README, QUICKSTART | 200 |
| `https://claude.com/claude-code` | README | 200, via a redirect to `/product/claude-code` |
| `https://counterparts.ai` | README | 200 |
| `https://github.com/mlapeter/counterparts/issues` | README | 200 |
| `https://github.com/mlapeter/counterparts.git` | QUICKSTART's clone line | 200, and the anonymous clone worked |
| `raw.githubusercontent.com/…/master/docs/images/dashboard-overview.png` | README | 200 `image/png` |
| 42 relative links, 33 distinct targets | the four docs | all resolve in the clone (the `#keys` anchors too); all 33 are 200 as `github.com/…/blob/master/<path>` |
| npm `repository.url` `git+https://github.com/mlapeter/counterparts.git` | `npm view` | 200 |
| npm `homepage` `https://github.com/mlapeter/counterparts#readme` | `npm view` | 200 |
| npm `bugs.url` `https://github.com/mlapeter/counterparts/issues` | `npm view` | 200 |
| npm README images `dashboard-brain.png`, `dashboard-flow.png`, `dashboard-overview.png` | 0.2.0's README | 200 `image/png`, all three |
| npm README relative links (`CONSTITUTION.md`, `docs/ELI5.md`, `docs/QUICKSTART.md`, `docs/module-map.md`) | 0.2.0's README | 200 on GitHub |
| `https://github.com/mlapeter/counterparts`, `/pulls`, `/commits/master`, `/issues` | — | 200 each |
| `https://raw.githubusercontent.com/mlapeter/counterparts/master/README.md` | — | 200 |
| `https://www.npmjs.com/package/counterparts` | — | 403 to curl whatever the user agent: npm's bot wall. Not a finding; `npm view` answers (0.2.0, `latest`). |

### C3. Install walk, from the README alone

**Setup.** A throwaway `HOME`, run through `env -i` so it looked like a plain terminal. The
only variables passed were `HOME`, `USER`, `SHELL`, `TERM`, `LANG`, `TMPDIR`, `BUN_INSTALL`,
`BUN_INSTALL_BIN`, `PATH=$HOME/.bun/bin:/usr/bin:/bin:/usr/sbin:/sbin` and
`COUNTERPARTS_REQUIRE_EXPLICIT_DIR=1`. The session's own Claude Code variables were not
passed. bun 1.3.10.

1. **`curl -fsSL https://bun.sh/install | bash`: not run, per the brief.** As a stand-in, I
   copied the existing bun 1.3.10 binary to `$HOME/.bun/bin/bun`, which is where that
   installer puts it.
2. **`bun add -g counterparts`:** "installed counterparts@0.2.0 with binaries: counterparts,
   counterparts-mcp, counterparts-hook, counterparts-dashboard", 1 package, 0.5 s. It
   installed under the throwaway `$HOME/.bun/install/global`. The package has no install
   scripts, and `engines.bun` is `>=1.3.0`, which matches the README.
3. **`counterparts --version`:** `counterparts 0.2.0`.
4. **`counterparts`**, the README's third line, run with no TTY and stdin closed. It prints
   the command list, ending "Not set up yet? `counterparts install` does it, and it is safe
   to run again." Exit 0, and nothing was written.
5. **`counterparts install --budget 9000 --name "Your Name"`**, QUICKSTART's scripted line
   exactly: refused, exit 2, "COUNTERPARTS_REQUIRE_EXPLICIT_DIR=1 and no configuration was
   named". That is my guard variable working, not a README fault; a stranger does not set
   it. Adding only `--dir` gets the same refusal, because the guard wants the config named
   too.
6. **The same line plus `--config $HOME/.counterparts/claude-code.json --dir
   $HOME/.counterparts/store`:** exit 0. It created the store, `claude-code.json` and
   `credentials.env` (mode 600), and seeded the identity core for "Your Name". It then says
   "Two steps left, and they are the HOST'S files, so they are printed, not applied". It
   prints the five-hook block for `settings.json` and the `claude mcp add` line, and ends
   "Or have it done for you: counterparts connect".
7. **`counterparts doctor`:** with no flags, the same guard refusal. With `--config`:
   **0 red, 1 amber, 2 off, 17 green.** The amber is Claude Code not connected; the two off
   lines are the two keys.
8. **`counterparts connect --config …`,** the step install printed. It reports "ok 5 hooks
   added to ~/.claude/settings.json", and warns that "`claude` is not on this PATH, so the
   MCP server was not registered" (my PATH had no `claude`), printing the line to run.
   **Surprise:** "(3 sessions are running the previous version's memory server — restart
   them.)" Those were the owner's real sessions, which it found through `ps`: it counts any
   running memory server, whatever its version or install. A stranger on a first install
   has none running, so the line would not appear.
9. **`counterparts doctor --config …` again:** 0 red, 1 amber (the hooks are connected; the
   MCP server is not registered), 2 off, 17 green.
10. **The throwaway home was removed.** The owner's own global bun install was not touched.

**README against reality.** The README's three lines do what it says: 0.2.0 installs and
reports its version. The mismatches are small:
- **QUICKSTART's scripted install does not connect Claude Code.** QUICKSTART says "To set it
  up from a script, with no questions: `counterparts install --budget 9000 --name "Your
  Name"`". In a script, install sets up the memory and then prints the hooks and the MCP
  line instead of applying them, so QUICKSTART's next step (restart, then `counterparts
  doctor`) shows amber. One added line would fix it: *"then `counterparts connect`"*. Also,
  `--budget 9000` is the default, so the flag does nothing.
- **The README says `counterparts` "walks you through setup".** It does at a terminal. With
  no terminal it prints the command list and the "Not set up yet?" line, which is the right
  behavior. Only someone scripting it would notice, and QUICKSTART covers scripts. I did not
  walk the interactive path.
- **A small product issue, not a doc one:** when `claude` is not on the PATH, doctor's amber
  fix line says "Run: counterparts connect", the command that just said it could not
  register. The real fix is the `claude mcp add …` line that connect prints.
- **Noted, nothing to do:** printed paths start with `/private/var/…`, which is macOS's
  real path for a temp directory. An ordinary `HOME` does not hit this.

### Graded findings

- **BLOCKER:** none.
- **MAJOR:** none.
- **MINOR:**
  1. **Your #73 comment** (`issuecomment-5549406590`) still shows your absolute home path,
     and it is public. It is cosmetic, because the username is the public handle. To fix
     it, edit it on the web, or save a scrubbed body and run `gh api -X PATCH
     repos/mlapeter/counterparts/issues/comments/5549406590 -F body=@<file>`.
  2. **QUICKSTART's scripted install** needs a "then `counterparts connect`" line (C3).
  3. **Doctor's fix line loops** when `claude` is not on the PATH (C3).
  4. **The npm page still shows 0.2.0's README.** It says "Not open to contributions yet",
     which the repository now contradicts, and it has no counterparts.ai link. npm's
     `homepage` is the GitHub readme. All of this changes on the next publish. If you want
     npm's homepage link to be the site, set `"homepage": "https://counterparts.ai"` in
     `package.json` before that publish.
  5. **Two images must stay on master for now.** 0.2.0's npm README shows
     `dashboard-brain.png` and `dashboard-flow.png` from `master`, but the repository's
     README now uses only the overview. Keep both files on master until a newer README is
     the one on npm.
  6. **`scan-history.ts` does not read tags.** It reads `refs/remotes/origin/*` and local
     heads. Today's one tag points at a commit on master, so nothing was missed, but a
     future tag on an unmerged commit would be.

### The site install block (draft for the `counterparts-site` session)

The README's install block, verbatim, then the two links and one sentence. The two lines
that install Counterparts are `bun add -g counterparts` and `counterparts`. The curl line is
the README's conditional line for people without bun. There is no version number, so the
block does not go stale.

```
Install — you need bun 1.3 or newer (https://bun.sh)

curl -fsSL https://bun.sh/install | bash     # if you don't have bun
bun add -g counterparts
counterparts

npm:    https://www.npmjs.com/package/counterparts
Source: https://github.com/mlapeter/counterparts

`counterparts` walks you through setup: your name, connecting Claude Code, and two optional API keys.
```

---


## Phase A report — 2026-09-23 (builder A, branch `go-public/phase-a`)

**Done, with one step left for the owner.** Every Phase A step ran except the branch deletion
(A8), which the permission classifier refused; the command is below. One thing to decide: the
next tarball packs 202 files, not 201 (A6). Default 5 is flipped, as the roadmap of 2026-09-23
decided: **contributions are open.** Your next three moves:

1. **Merge this PR.**
2. **Delete the merged remote branches** (the A8 command below).
3. **Run step B.**

**A1. Baseline** (master `37e9518`, the tip on 2026-09-23). Suite 3,182 pass / 0 fail, `tsc` clean,
`npm pack --dry-run` 201 files. The tree scan against the table below (run at `78c63e4`): owner-handle
62→64, tilde-path 943→951, total 2,172→2,182 in 154 files. Every new hit is in the three pages that
landed since (HANDOFF's top section, this plan, the embeddings research). I read them; none is a new
class. History scan `--worktree`: 363→365 blobs, 185→187 with findings, 54→56 non-entropy hits. The
two new hits are this page quoting the fixture names (`AKIA…`, `sk-a…`).

**Scanner classes, before → after this branch** (`tools/audit/scan-personal.ts`):

| class | before | after |
|---|---|---|
| owner-handle | 64 | 20 |
| owner-name | 121 | 121 |
| owner-site | 2 | 2 |
| absolute-home-path | 55 in 23 files | 14 in 9 files (0 in `docs/`) |
| tilde-path | 951 | 968 |
| live-store-path | 964 | 955 |
| uuid-session-id | 9 | 6 |
| short-session-id | 3 | 3 |
| run-id | 8 | 8 |
| scratchpad-path | 3 | 0 |
| claude-projects-path | 2 | 2 |
| **total** | **2,182 in 154 files** | **2,099 in 153 files** |

The owner-handle count fell because each `/Users/<owner>` also counted as a handle. The 20 left
are the GitHub handle in URLs, `package.json`, a few commands, and test aliases, all under the
09-04 ruling. History scan `--worktree` after: 365 blobs, 186 with findings, the same 56
non-entropy hits.

**A2. Home paths.** 44 lines in 15 docs. `/Users/<owner>/` became `~/`. The three session
scratchpad paths became `<scratchpad>/`. The two lines where the literal `homedir()` value was the
point now read `/Users/<owner>`. The one quoted sqlite URI uses `$HOME`, so it still runs. The 14
absolute-home-path hits left are all placeholders (`/Users/test`, `/Users/x`, `/Users/you`, …):
eleven in tests, one in `tools/replay/README.md`, and two in `src/` files a wave-1 builder owns,
so I did not touch them:
- `src/adapters/cli/wire.ts:152` (B2): the comment example `/Users/mike/.claude/settings.json`.
- `src/core/remember/NOTES.md:21` (B3): `/Users/x/proj`.

No other `src/` file had a hit. The test fixtures and the tunables comment's v1 session id are
untouched.

**A3. Prompts out, index in.** `git rm docs/overnight-prompt-2026-09-04.md
docs/launch/coordinator-log-2026-09-04-07.md`. New `docs/README.md` says what each class of
document is, and names `docs/ROADMAP.md`, which arrives with PR #184. Two things for the owner to
see:
- `docs/LAUNCH-STATUS.md:1248` records *"Owner ruled 2026-09-10: it stays in the tree"* for the
  overnight prompt. This plan's choice 1 (accepted 2026-09-23) supersedes that ruling. If you meant
  to keep the prompt, revert that one deletion before merging.
- `docs/plan-parallel-rebuild-2026-09-18.md` §8 is also a coordinator prompt ("the prompt a fresh
  session runs from"). The plan did not name it, so it stays.

**A4. README.** The counterparts.ai link is in the opening block and under "Read next", beside
`docs/README.md` and the new `CONTRIBUTING.md`. "License and contributing" now says contributions
are welcome and asks for an issue first on anything touching a store or someone's config.
`CONTRIBUTING.md` is one page covering the suite, the two safety rules, `CONSTITUTION.md` as the
only standing authority, `CONTRACT.md` / `INTERFACE-GAPS.md`, issue-first for stores and config,
small PRs with tests, and MIT. The README names bansai once, in "Lineage", as prose. `git grep
'](~'` finds no Markdown link to a local path anywhere. The image URLs are unchanged.

**A5. PR bodies.** I scanned all 184 bodies for `/Users/`, emails and key shapes. Only **#142**
had one: `shared checkout /Users/<owner>/counterparts:` in a code block became `~/counterparts`.
I edited that body and nothing else, and its trailer stays. #19, the case known on 09-04, is
already clean. Outside the brief, I also scanned the 29 PR conversation comments. One, **your own
comment on #73** (`issuecomment-5549406590`), quotes a stderr line with `/Users/<owner>/.counterparts/store`.
I did not edit it. To fix it, edit the comment on the web, or save a scrubbed copy of the body and
run `gh api -X PATCH repos/mlapeter/counterparts/issues/comments/5549406590 -F body=@<file>`.
There are no issues and no review comments.

**A6. Pack.** `LICENSE` is MIT (2026 Mike LaPeter). `package.json` has `"license": "MIT"`, no
`"private"`, and `files`/`bin` as published. The stale-words grep is empty. The checklist's step 5
ignores are already in `.gitignore`. `npm pack --dry-run` counted 201 files on master and
**202 on this branch**. npm packs a README found in any packed directory, and `docs/` is packed
for `QUICKSTART.md`, so the new `docs/README.md` ships in the next tarball. Its links to
`../CONTRIBUTING.md` and the dated records will not resolve inside the package. I did not change
it, per the brief. If you want it out, add `"!docs/README.md"` to `files` in `package.json`.
The published 0.2.0 is not affected.

**A7. Repo metadata: done.** The description is the plan's wording, the homepage is
`https://counterparts.ai`, and the topics are `ai-memory, bun, claude-code, local-first, mcp,
memory, sqlite, typescript`. Visibility is still PRIVATE.

**A8. Branches: refused.** `git push origin --delete <165 names>` was refused by the permission
classifier ("unverifiable deletion scope"). I did not retry it. **Nothing was deleted.** The 165
branches are every ref `--merged origin/master` except `master`. To run it yourself from
`~/counterparts`, read the list before the last line:

```sh
git fetch origin --prune
open=$(gh pr list --state open --json headRefName -q '.[].headRefName')
git for-each-ref --merged origin/master --format='%(refname:strip=3) %(objectname)' refs/remotes/origin \
  | awk -v m="$(git rev-parse origin/master)" '$2 != m {print $1}' \
  | grep -vxE 'master|HEAD' | grep -vxF "$open" > /tmp/merged-branches.txt
wc -l < /tmp/merged-branches.txt; cat /tmp/merged-branches.txt   # 165 on 2026-09-23, more once wave 1 merges
git push origin --delete $(cat /tmp/merged-branches.txt)
```

The `awk` line keeps any branch that sits exactly at master's tip, such as a builder's
freshly pushed branch. `grep -vxF "$open"` keeps any branch with an open PR. **Survivors:** the
unmerged `dashboard/flat`, `design/parallel-run-contract` and `docs/2026-09-18-morning`;
`docs/roadmap-2026-09-23` (PR #184, open); `go-public/phase-a` (this PR); and any wave-1 branch
still open.

**A9. Gate.** I ran it on a clean detached checkout of `d840544`, the branch head before this
report: `bun install --frozen-lockfile`, then `bun test`: **3,182 pass, 0 fail**, 51 files, 82 s.
`tsc --noEmit` is clean and the pack count is 202. The commit after `d840544` adds only this
report and the note on choice 5.

---

## The plan (as written 2026-09-22)

*The owner's decision, 2026-09-22: **make `mlapeter/counterparts` public, as it stands, history
included.** This re-affirms the 2026-09-04 ruling in `flip-checklist.md` (first names, business
names, the author identity on every commit — all fine). What changed since that checklist: the
repository grew from 183 to 872 commits and from 10 to 61 docs, the package is on npm (0.1.0 on
09-21, 0.2.0 on 09-22) with `package.json` links that 404 at the private repo, and the site
`counterparts.ai` is live. This page maps the checklist onto today and says which steps a session
runs unattended and which one is the owner's.*

**Read `flip-checklist.md` first; this page does not restate its reasoning.** Everything below is a
working default, revisable when it fights `CONSTITUTION.md`.

## What was checked tonight, so the fresh session compares rather than re-decides

Run on master `78c63e4` (docs-only over 0.2.0's code at `31659fe`):

| check | result |
|---|---|
| `tools/audit/scan-personal.ts` (tree) | 2,172 hits in 152 files. Classes: owner-name 121, owner-handle 62, owner-site 2, absolute-home-path **55 in 23 files**, tilde-path 943, live-store-path 964, uuid-session-id 9 (test fixtures), short-session-id 3 (one v1 session id in a tunables comment + fixtures), run-id 8, scratchpad-path 3, claude-projects-path 2. All name/handle/dotfile hits fall under the ruling. **The absolute home paths are the only class to scrub** — cosmetic (the username is already public via the handle), but a stranger should not read `/Users/<name>/…` in a doc. |
| `tools/audit/scan-history.ts --worktree` | 185 of 363 blobs with findings; 54 non-entropy hits, every one a test fixture (`AKIAIOSFODNN7EXAMPLE`, `sk-ant-not-a-real-key…`, `AIzaSyD-1234…`, the encode gate's fixtures) or source that NAMES a credential variable (`token=`, `credentialsFile`). |
| `tools/audit/scan-history.ts` (all 872 commits, 3,630 blobs, 185 MB) | 2,388 blobs with findings; every non-entropy hit outside `test/` is source naming a variable, the deleted 09-04 audit document quoting the fixtures it classified, or a dangling local-only blob that was never pushed. **Zero true positives, same as 09-04.** |
| `git grep` for emails / memory ids at HEAD | none (the author's own gmail appears once in history, in a line ABOUT commit-author identity; it is on every commit anyway and on the site). |
| `credentials.env`, `.npmrc`, `.pem` ever committed | never. |
| README | rewritten 2026-09-22 for 0.2.0 (`ce0e9bb`); "Status, honestly" current; no stale wire/unwire, no "coming soon". **Gap: no link to counterparts.ai.** Its three dashboard images point at `raw.githubusercontent.com/…/master/…`, which 404 on the npm page until the flip. |
| GitHub metadata | description set (long, ends "(v2)"); homepage EMPTY; topics NONE; 181 PRs; ~30 remote branches, most merged. |
| Site | `counterparts.ai` says "coming soon", email signup, no install line, no npm or repo link. |

## Owner choices, with the defaults this plan takes (override by saying so)

1. **The working record stays in the tree** — HANDOFF, the findings, LAUNCH/PARALLEL-RUN status,
   the adversarial reviews, IMPROVEMENTS, the harvest. It is the most interesting part of the
   project for the people he is about to talk to. The 09-04 ruling removed *prompts* (operating
   instructions to agents) from the tree; the same rule applies to `docs/overnight-prompt-2026-09-04.md`
   and `docs/launch/coordinator-log-2026-09-04-07.md` — **default: `git rm` those two**, keep the rest,
   and add `docs/README.md`, one page that says what each class of document is ("the workshop with
   the lights on") so a stranger knows what they are reading. *Alternative he may prefer: move the
   working record under `docs/workshop/` instead — same effect, more path churn.*
2. **Merged remote branches are deleted** before the flip (they become public otherwise). Unmerged
   ones (`dashboard/flat` is the known one) stay and are listed in the report.
3. **Repo metadata:** description → `Memory for AI, modeled on human memory — a local-first memory
   layer that turns Claude Code into a counterpart`; homepage → `https://counterparts.ai`; topics →
   `ai-memory, memory, claude-code, mcp, sqlite, bun, typescript, local-first`. His wording wins.
4. **PR bodies keep their "Generated with Claude Code" trailers** (the project says so itself).
   Only bodies carrying an absolute home path are edited (PR #19 was the known one on 09-04; the
   session re-scans all 181).
5. ~~**Contributions stay closed** (README says so); issues welcome. No CONTRIBUTING file yet.~~
   **Flipped 2026-09-23 (roadmap): contributions are open.** See the Phase A report at the top.
   `CONTRIBUTING.md` exists.

## Phase A — before the flip (a session, unattended)

Work on a branch from master in a worktree, never in `~/counterparts`'s working tree while it is
on master. One PR at the end; the owner merges it (the permission classifier refuses `gh pr merge`
from a session, and once refused a plain `git checkout master` in the same breath — leave the tree
where it sits and say so).

- **A1. Baseline.** Clean detached checkout of master; `bun test`, `tsc`; run both scanners as in
  checklist step 2 and diff the class counts against the table above. **Anything new is the thing
  to read.**
- **A2. Scrub absolute home paths** in `docs/` and `src/` comments (55 hits, 23 files): `/Users/<name>/`
  → `~/`; scratchpad and `.claude/projects` paths → a generic placeholder. Do not touch test
  fixtures or the tunables comment's session id (it is v1's, on the record as evidence). Re-run
  `scan-personal.ts --list absolute-home-path` → expect 0.
- **A3. Prompts out, index in** (choice 1): `git rm` the two prompt/log files; write `docs/README.md`.
- **A4. README:** add the site link in the opening block and under "Read next"; confirm every
  `~/bansai` mention reads as lineage, not a link (checklist step 11); leave the image URLs (they
  resolve after the flip).
- **A5. PR bodies:** `gh pr list --state all --limit 200 --json number,title,body`, scan for
  `/Users/`, emails, key shapes; `gh pr edit N --body-file` only where a home path appears; report
  the numbers touched.
- **A6. Packaging pack** (checklist step 4): LICENSE, `package.json` fields, `npm pack --dry-run`
  → 201 files, same as the published 0.2.0.
- **A7. Repo metadata** (choice 3) via `gh repo edit --description … --homepage …` and
  `gh repo edit --add-topic …`. If the classifier refuses, print the exact commands for the owner.
- **A8. Branches** (choice 2): list `git branch -r --merged origin/master`, delete those, list the
  survivors.
- **A9. Gate and hand-off:** suite + `tsc` on a clean checkout of the branch; open the PR; write the
  report (what changed, scanner deltas, branches deleted, PR bodies edited, anything the classifier
  refused) at the TOP of this file in the same PR; then stop with two lines for the owner: *merge
  the PR*, then *run step B*.

## Phase B — the flip (the owner, one command)

Precondition: Phase A's PR is merged and master is what he wants strangers to see (checklist step
7: `git ls-files | wc -l`, skim the top level and `docs/`).

```sh
gh repo edit mlapeter/counterparts --visibility public --accept-visibility-change-consequences
```

Irreversible in effect (forks, archivers, search engines). **No session runs this.**

## Phase C — after the flip (a session, unattended)

- **C1.** Anonymous clone into a temp dir (never `~/counterparts`), re-run both scanners with the PR
  head refs fetched (checklist step 10); read the shape once.
- **C2.** Logged-out link check: the README's links, the npm page's repository / homepage / bugs
  links (they resolve now), the three README images on npm.
- **C3.** Install walk from the README alone in a throwaway HOME (`HOME`, `BUN_INSTALL`,
  `BUN_INSTALL_BIN` overridden; bun by absolute path; `COUNTERPARTS_REQUIRE_EXPLICIT_DIR=1`):
  `bun add -g counterparts` → `counterparts --version` prints 0.2.0 → the scripted install path.
- **C4.** Report, then the site: an install block (the two install lines, the npm link, the repo
  link) on counterparts.ai — a session in `~/counterparts-site`, reviewed locally first, pushed to
  `main` only on his word (push = deploy). That is the last thing before he mentions it anywhere.
