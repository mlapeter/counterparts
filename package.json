{
  "name": "counterparts",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Memory that turns AI tools into counterparts - a local-first, human-memory-modeled memory layer for AI.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "playwright": "1.62.1",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  }
}
